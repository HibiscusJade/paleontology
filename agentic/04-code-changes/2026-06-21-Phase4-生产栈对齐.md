# Phase 4 — 生产栈对齐（范围调整）

**日期**：2026-06-21  
**状态**：后端暂停；前端以 React 客户端原型为准

## 范围调整说明

按最新指示：

- **不写 / 不改 Java 后端**（`PaleontologicalResearchBack`）
- **前端仅作客户端原型展示**（`paleontology-website-latest`、`paleontology-admin-latest`）
- 已撤销本轮后端改动

## 后端撤销记录

| 操作 | 文件 | 说明 |
|------|------|------|
| 删除 | `PaleontologicalResearchBack/.../PaleoConstants.java` | 本轮新增，已删除 |
| 无变更 | `PaleoConferenceRegistrationService.java` 等 | 中断前未写入 |

验证：`PaleontologicalResearchBack` 工作区 `git status` 为 clean。

## 前端原型现状（Phase 2 + 3 已交付）

| 子项目 | 路径 | 能力 |
|--------|------|------|
| 用户端原型 | `paleontology-website-latest/` | 四类费用、总学会虚拟绑定、入会/退会、localStorage 双写 |
| 管理端原型 | `paleontology-admin-latest/` | 三级统计实收、ZIP 导出、分会权限、审核联调 |

生产 Vue 栈（`PaleontologicalResearchAdminFront` / `PaleontologicalResearchFront`）与后端 API 对齐 **暂缓**，待后端契约稳定后再接。

## 验证（React 原型）

```bash
cd paleontology-website-latest && pnpm check && pnpm build
cd paleontology-admin-latest && pnpm check && pnpm build
```

## 遗留（待后端 + 生产 Vue 阶段）

- B-1 ~ B-6 后端契约补齐
- A-3 管理端 ZIP 走后端（当前 React 原型为前端 JSZip）
- F-1 ~ F-4 用户端 Vue 接 API
- `agentic/03-api-docs/paleo-0617-api.md` 定稿
- 主仓库 `.gitmodules` 子模块指针
