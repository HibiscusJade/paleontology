# 会员入会退会与会费审核实现

## 基本信息

| 字段 | 内容 |
|---|---|
| 日期 | 2026-06-18 |
| 变更目标 | 继续推进确定需求，补齐会员入会/退会申请、申请书模板、会员费凭证与发票审核骨架 |
| 关联 PRD | `agentic/01-prd/2026-06-18-确定需求实现清单-PRD.md` |
| 当前状态 | 已完成第二批实现 |

## 变更范围

### 后端服务

- 新增会员入会/退会申请实体、Mapper、Service 和 Controller。
- 新增会员费缴费实体、Mapper、Service 和 Controller。
- 新增入会/退会申请书模板实体、Mapper、Service 和 Controller。
- 新增审核接口：
  - `/paleo/membership/applications/{applicationId}/review`
  - `/paleo/membership/payments/{paymentId}/review`
- 新增会员费统计接口：
  - `/paleo/membership/payments/stats`
- 新增 SQL 脚本 `V0.0.3__Paleo_membership_flow.sql`。

### 后台管理前端

- 扩展 `src/api/paleo/confirmed.js`，接入会员申请、会员费、申请书模板接口。
- 扩展确定需求看板，新增：
  - 入会/退会申请列表。
  - 会员费凭证/发票列表。
  - 申请书模板列表。
  - 累计会员费统计。

### 用户前端

- 会员服务区增加入会申请、申请审核、会员费凭证、发票终审、退会申请流程。
- 用户前端数据增加 `membershipFlow`，展示入会/退会与会费审核步骤。

## 验证记录

- `mvn compile`（后端全量）：通过；存在 Maven 本地仓库 tracking 权限 warning，不阻塞编译。
- `npm run build:prod`（后台管理前端）：通过；存在项目既有 Flowable 导出缺失、CSS 顺序和包体积 warning，不阻塞构建。
- `npm run build`（用户前端）：通过。

## 后续事项

- 后续继续推进文件上传真实接入、状态机约束、分会管理员数据权限过滤、批量导出和智能识别辅助。
