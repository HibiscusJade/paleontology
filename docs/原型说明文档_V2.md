# 中国古生物学会网站 — 原型说明文档 V2

> **版本：** V2（两阶段审核与规则细化）
> **最后更新：** 2026-06-10
> **基于：** 原 原型说明文档.md + 修改计划_两阶段审核与规则细化.md Phase 1-4
> **代码目录：** `client/src/`
>
> **V2 核心变更：**
> 1. 会员费/会议费审核均改为两阶段（凭证初审 → 发票终审）
> 2. 发票从选传改为必传，新增7工作日宽限期
> 3. 会员到期后会议资格分级处理
> 4. 会员费金额改为后台可配置参数

---

## 目录

1. [项目总览](#1-项目总览)
2. [架构设计](#2-架构设计)
3. [设计系统](#3-设计系统)
4. [会员体系（核心）](#4-会员体系核心)
   - [4.1 全局用户状态模型](#41-全局用户状态模型)
   - [4.2 账号注册](#42-账号注册)
   - [4.3 登录与登出](#43-登录与登出)
   - [4.4 统一会费缴纳（两阶段审核）](#44-统一会费缴纳两阶段审核)
   - [4.5 分会绑定机制](#45-分会绑定机制)
   - [4.6 会议注册费缴纳（两阶段审核）](#46-会议注册费缴纳两阶段审核)
   - [4.7 参会信息与摘要提交](#47-参会信息与摘要提交)
   - [4.8 个人中心](#48-个人中心)
   - [4.9 通知系统](#49-通知系统)
   - [4.10 权限模型汇总](#410-权限模型汇总)
5. [学会服务页面深度解析](#5-学会服务页面深度解析)
6. [其他学会主站页面](#6-其他学会主站页面)
7. [党建文化子系统](#7-党建文化子系统)
8. [全局组件](#8-全局组件)
9. [状态管理与数据流](#9-状态管理与数据流)
10. [路由表](#10-路由表)
11. [模拟数据与演示流程](#11-模拟数据与演示流程)
12. [已知问题与待改进](#12-已知问题与待改进)
13. [V1→V2 变更明细](#13-v1v2-变更明细)

---

## 1. 项目总览

### 1.1 技术栈

| 技术 | 版本/方案 |
|------|----------|
| 前端框架 | React 19 |
| 构建工具 | Vite 7 |
| 类型系统 | TypeScript 5.6 |
| CSS框架 | Tailwind CSS 4 |
| UI组件库 | shadcn/ui (New York style) |
| 路由 | wouter (支持 hash/pushState 双模式) |
| 状态管理 | React Context (`MembershipContext`) |
| 图标 | Material Symbols Outlined + lucide-react |
| 表单 | react-hook-form + zod |
| 动画 | framer-motion |
| 图表 | recharts |

### 1.2 项目定位

当前原型是一个 **纯前端 SPA**，无后端 API。所有数据通过 `localStorage` 模拟持久化，所有交互（注册、缴费、审核、绑定）均在前端模拟完成。目标是**验证交互流程**，为后续后端开发提供参考。

### 1.3 V2 新增模块

| 模块 | 文件 | 说明 |
|------|------|------|
| 共享常量 | `shared/constants.ts` | 会员费配置、状态枚举、标签映射、分会ID映射、会议-分会映射、宽限期常量 |
| 两阶段状态机 | `MembershipContext.tsx` | 8种会议状态 + 9种会员状态，含初审/终审分离 |
| 宽限期系统 | `MembershipContext.tsx` | 工作日截止日计算、自动逾期检测、截止日延期 |
| 会员到期处理 | `MembershipContext.tsx` | 分级处理（保留/冻结/删除）、续费恢复逻辑 |

---

## 2. 架构设计

### 2.1 双站点结构

```
单一 React SPA
├── 学会主站（Society Main Portal）
│   ├── 首页 (/)
│   ├── 学会服务 (/services)        ← 核心交互页面（两阶段缴费）
│   ├── 个人中心 (/personal-center)  ← 新状态标签
│   └── ... 其他页面
│
└── 党建文化子系统（Party Culture Sub-system）
    └── 12个子页面
```

### 2.2 布局组件层级

```
App
├── ErrorBoundary
│   └── ThemeProvider (强制light模式)
│       └── MembershipProvider (全局状态 — V2两阶段)
│           └── TooltipProvider
│               └── Toaster (sonner)
│                   └── AppRouter (Switch)
│                       └── PartyLayout (所有页面共用)
│                           ├── TopNavBar
│                           │   └── 通知铃铛（含V2新增12种通知类型）
│                           └── ...
```

---

## 3. 设计系统

### 3.1 状态颜色体系（V2 新增）

| 状态类型 | Tailwind 类 | 语义 |
|---------|------------|------|
| 未缴费 | `bg-gray-50 text-gray-500 border-gray-200` | 灰色 — 待操作 |
| 凭证初审中 | `bg-yellow-50 text-yellow-700 border-yellow-200` | 黄色 — 等待中 |
| 凭证被驳回 | `bg-red-50 text-red-700 border-red-200` | 红色 — 需修正 |
| 待上传发票 | `bg-blue-50 text-blue-700 border-blue-200` | 蓝色 — 进行中 |
| 发票逾期 | `bg-orange-50 text-orange-700 border-orange-200` | 橙色 — 警告 |
| 发票终审中 | `bg-yellow-50 text-yellow-700 border-yellow-200` | 黄色 — 等待中 |
| 发票被驳回 | `bg-red-50 text-red-700 border-red-200` | 红色 — 需修正 |
| 已确认/已激活 | `bg-green-50 text-green-700 border-green-200` | 绿色 — 完成 |
| 已过期 | `bg-gray-100 text-gray-500 border-gray-300` | 灰色 — 失效 |

---

## 4. 会员体系（核心）

> **这是整个系统最核心的业务逻辑。V2 最重大的变化：缴费审核从一阶段改为两阶段。**

### 4.1 全局用户状态模型

```
用户状态层级（V2 两阶段版）:

  ┌─────────────────────────────────────────────────────────┐
  │ 第3层：会议参与状态                                       │
  │   conferenceRegs[confId].status                          │
  │   unpaid → voucher_submitted → invoice_pending           │
  │         → invoice_overdue → invoice_submitted            │
  │         → confirmed                                      │
  │   (可驳回: voucher_rejected, invoice_rejected)           │
  ├─────────────────────────────────────────────────────────┤
  │ 第2层：分会绑定状态                                       │
  │   boundBranches: string[]                                │
  │   条件：societyMembership.status === "active"             │
  │   到期时自动清空                                          │
  ├─────────────────────────────────────────────────────────┤
  │ 第1层：学会会员状态                                       │
  │   societyMembership.status                               │
  │   not_member → voucher_submitted → invoice_pending       │
  │             → invoice_overdue → invoice_submitted        │
  │             → active → expired                           │
  │   (可驳回: voucher_rejected, invoice_rejected)           │
  ├─────────────────────────────────────────────────────────┤
  │ 第0层：账号状态                                           │
  │   isLoggedIn: boolean                                    │
  │   currentUser: User | null                               │
  └─────────────────────────────────────────────────────────┘
```

#### V2 数据模型定义

```typescript
// ===== 共享常量（shared/constants.ts）=====

// 会员费配置
export const MEMBERSHIP_FEE_CONFIG = {
  default: { standard: 200, student: 100, corporate: 5000 },
  currentYear: 2026,
} as const;

export const CONFERENCE_STATUS = {
  UNPAID: "unpaid",
  VOUCHER_SUBMITTED: "voucher_submitted",
  VOUCHER_REJECTED: "voucher_rejected",
  INVOICE_PENDING: "invoice_pending",
  INVOICE_OVERDUE: "invoice_overdue",
  INVOICE_SUBMITTED: "invoice_submitted",
  INVOICE_REJECTED: "invoice_rejected",
  CONFIRMED: "confirmed",
} as const;

export const MEMBERSHIP_STATUS = {
  NOT_MEMBER: "not_member",
  VOUCHER_SUBMITTED: "voucher_submitted",
  VOUCHER_REJECTED: "voucher_rejected",
  INVOICE_PENDING: "invoice_pending",
  INVOICE_OVERDUE: "invoice_overdue",
  INVOICE_SUBMITTED: "invoice_submitted",
  INVOICE_REJECTED: "invoice_rejected",
  ACTIVE: "active",
  EXPIRED: "expired",
} as const;

// 运行时读取配置
export function getMembershipFee(memberType: string = "standard"): number { ... }

// 分会ID映射（统一）
export const BRANCH_MAP: Record<string, string> = {
  gwjzdwxfh: "古无脊椎动物学分会",
  // ... 11 entries
};

// 会议→分会映射
export const CONFERENCE_BRANCH_MAP: Record<string, string> = {
  "conf-1": "wtxfh", "conf-2": "gzwxfh", // ... 9 entries
};

// ===== 会议注册 (ConferenceReg) V2 =====
interface ConferenceReg {
  status: ConferenceStatus;  // 8种状态
  // 第一阶段：凭证
  paymentVoucher?: string;
  voucherSubmitTime?: string;
  voucherAuditTime?: string;
  voucherRejectReason?: string;
  // 第二阶段：发票
  invoiceUrl?: string;
  invoiceSubmitTime?: string;
  invoiceAuditTime?: string;
  invoiceRejectReason?: string;
  invoiceDeadline?: string;
  invoiceExtendedDeadline?: string;
  // 参会信息
  name: string;
  gender: "男" | "女";
  unit: string;
  role: "学生" | "教师" | "嘉宾";
  accommodation: "单间" | "双人间" | "自行安排";
  session: string;
  presentationType: "口头报告" | "展板报告" | "仅参会";
  reportTitle?: string;
  abstractFileName?: string;
  lastUpdated?: string;
  // 会员到期冻结标记
  frozenDueToExpiry?: boolean;
}

// ===== 学会会员 (SocietyMembership) V2 =====
interface SocietyMembership {
  status: MembershipStatus;  // 9种状态
  amount?: number;            // 缴费金额（已废弃，从配置读取）
  expiryDate?: string;
  voucherRejectReason?: string;
  invoiceRejectReason?: string;
  invoiceDeadline?: string;
  invoiceExtendedDeadline?: string;
  frozenDueToExpiry?: boolean;
  history: PaymentRecord[];
}

// ===== 缴费记录 (PaymentRecord) V2 =====
interface PaymentRecord {
  id: string;
  type: "society_fee" | "conference_fee";
  targetName: string;
  targetId?: string;
  amount: number;
  voucherUrl: string;
  voucherSubmitTime: string;
  voucherAuditTime?: string;
  invoiceUrl?: string;
  invoiceSubmitTime?: string;
  invoiceAuditTime?: string;
  status: "voucher_submitted" | "voucher_rejected" | "invoice_submitted"
        | "invoice_rejected" | "approved" | "rejected";
  rejectReason?: string;
  rejectPhase?: "voucher" | "invoice";  // 区分驳回阶段
}
```

---

### 4.4 统一会费缴纳（两阶段审核）

> **V2 核心变更：从一阶段审核改为两阶段（凭证初审 → 发票终审），发票从选传改为必传。**

#### 4.4.1 V2 完整状态机

```
                    ┌─────────────┐
        自动注册后   │ not_member  │
      ┌───────────→ │             │
      │             └──────┬──────┘
      │                    │ submitMembershipVoucher()
      │                    │ (上传凭证，提交初审)
      │                    ▼
      │             ┌─────────────────┐
      │     ┌───────│ voucher_submitted│  ← 凭证已上传，待初审
      │     │       └────────┬────────┘
      │     │                │
      │     │    ┌───────────┼───────────┐
      │     │    │                       │
      │     │    │ 初审通过               │ 初审驳回
      │     │    │ simApproveSociety     │ simRejectSociety
      │     │    │ Voucher()             │ Voucher(reason)
      │     │    │                       │
      │     │    ▼                       ▼
      │     │ ┌────────────────┐  ┌──────────────────┐
      │     │ │ invoice_pending │  │ voucher_rejected │
      │     │ │ (设置发票截止日)│  │ (重新上传凭证)    │
      │     │ └───────┬────────┘  └────────┬─────────┘
      │     │         │                    │
      │     │         │ submitMembership   │ submitMembership
      │     │         │ Invoice()          │ Voucher()
      │     │         │ (上传发票)          │
      │     │         │                    │
      │     │    ┌────┼────────┐           │
      │     │    │             │           │
      │     │    │ 正常上传     │ 逾期       │
      │     │    ▼             ▼           │
      │     │ ┌──────────┐ ┌──────────┐   │
      │     │ │ invoice_ │ │ invoice_ │   │
      │     │ │ submitted│ │ overdue  │   │
      │     │ └────┬─────┘ └────┬─────┘   │
      │     │      │            │ 仍可上传  │
      │     │      │ ┌──────────┘          │
      │     │      │ │                     │
      │     │ ┌────┼─┼─────────┐           │
      │     │ │    │           │           │
      │     │ │ 终审通过        │ 终审驳回    │
      │     │ │                │           │
      │     │ ▼                ▼           │
      │     │ ┌──────────┐ ┌──────────────┐│
      │     │ │  active  │ │ invoice_     ││
      │     │ │ (有效期   │ │ rejected     ││
      │     │ │  1年)    │ │ (重新上传发票)││
      │     │ └────┬─────┘ └──────────────┘│
      │     │      │                       │
      │     │      │ 1年后到期              │
      │     │      ▼                       │
      │     │ ┌──────────┐                │
      │     │ │ expired  │                │
      │     │ └────┬─────┘                │
      │     │      │ "立即续费"             │
      │     └──────┼───────────────────────┘
      │            │ handleMembershipRenewal()
      │            │ (回到 voucher_submitted)
      │            │
      └────────────┘
```

#### 4.4.2 缴费流程详细交互（4步，V2新增Step 4）

```
进入条件：用户在会员服务Tab点击"立即申请入会"

STEP 1: 确认账户与金额
  ┌─ 左栏: 缴费项目明细 ───────┐  ┌─ 右栏: 收款账户 ─────┐
  │ 缴费项目  中国古生物学会会费 │  │ 户名  中国古生物学会    │
  │ 会员类型  个人会员(普通)    │  │ 开户行 工商银行...      │
  │ 有效期    1年             │  │ 账号  4301 0108...     │
  │ ┌─────────────────────┐  │  └─────────────────────┘
  │ │应缴会费  ¥ {配置值} 元│  │
  │ └─────────────────────┘  │
  └───────────────────────────┘
  ⚠ 请备注 "{姓名} 学会会费"
  → "已完成汇款，下一步上传凭证"

STEP 2: 汇款转账确认
  确认已向学会账户汇入对应金额会费
  → "汇款已完成，去上传凭证"

STEP 3: 上传凭证（阶段一）
  1. 汇款/转账凭证回单（必传）*
  ┌ - - - - - - - - - - - - - - - ┐
  │  ☁ 点击模拟上传转账电子回单    │
  │  支持 JPG/PNG，≤ 5MB          │
  └ - - - - - - - - - - - - - - - ┘
  → "提交入会申请" → status = "voucher_submitted"

  【V2新增：初审阶段提示】
  "凭证已提交，财务初审中（通常1-3个工作日）。
   初审通过后，请上传电子发票完成终审。"

STEP 4: 上传发票（阶段二）【V2新增】
  前置条件：初审通过 → status = "invoice_pending"
  ┌──────────────────────────────────┐
  │  📋 上传电子发票                  │
  │                                  │
  │  ⏰ 截止日：2026-06-19（还有7天）  │  ← 倒计时
  │                                  │
  │  ┌ - - - - - - - - - - - - - - ┐│
  │  │  📄 点击上传电子发票          ││
  │  │  支持 JPG/PNG/PDF，≤ 10MB   ││
  │  └ - - - - - - - - - - - - - - ┘│
  │                                  │
  │  [上一步]  [提交发票进入终审]      │
  └──────────────────────────────────┘
  → submitMembershipInvoice() → status = "invoice_submitted"
  → OCR金额比对 → 一致后进入终审
```

#### 4.4.3 V2 各状态下UI

```
[not_member]:
  "您尚未申请成为学会会员"
  [ 立即申请入会（¥{配置值}/年） ]

[voucher_submitted]:  ← V2 新
  ⏳ 凭证初审中
  凭证已提交，待财务初审(1-3工作日)
  [演示] [✅ 模拟初审通过] [❌ 模拟初审驳回]

[voucher_rejected]:  ← V2 新
  ✗ 凭证审核被驳回
  驳回原因：{reason}
  [ 重新上传凭证 ]

[invoice_pending]:  ← V2 新
  📋 待上传发票
  凭证初审已通过，请于{deadline}前上传电子发票
  截止日倒计时: 还有X天
  [ 上传发票 ]
  [演示] [✅ 模拟终审通过]

[invoice_overdue]:  ← V2 新
  ⚠️ 发票上传逾期
  截止日已过，请尽快上传以完成入会
  [ 上传发票 ]

[invoice_submitted]:  ← V2 新
  ⏳ 发票终审中
  发票已提交，待财务终审
  [演示] [✅ 模拟终审通过] [❌ 模拟终审驳回]

[invoice_rejected]:  ← V2 新
  ✗ 发票终审被驳回
  驳回原因：{reason}
  [ 重新上传发票 ]

[active]:
  ✓ 会员资格有效
  有效期至：{expiryDate}
  [ 提前续费 ]

[expired]:
  会员资格已过期
  [ 立即续费 ]
```

#### 4.4.4 V2 模拟审核函数

```typescript
// 两阶段审核 — 8个函数（替换旧的2个）

// 会员初审
simApproveSocietyVoucher():     // voucher_submitted → invoice_pending (设置invoiceDeadline)
simRejectSocietyVoucher(reason): // voucher_submitted → voucher_rejected

// 会员终审
simApproveSocietyInvoice():     // invoice_submitted → active (设置expiryDate)
simRejectSocietyInvoice(reason): // invoice_submitted → invoice_rejected

// 会议初审
simApproveConferenceVoucher(confId):     // voucher_submitted → invoice_pending
simRejectConferenceVoucher(confId, reason): // voucher_submitted → voucher_rejected

// 会议终审
simApproveConferenceInvoice(confId):     // invoice_submitted → confirmed
simRejectConferenceInvoice(confId, reason): // invoice_submitted → invoice_rejected
```

---

### 4.5 分会绑定机制

> **V2 新增：会员到期后自动解除所有分会绑定。** 续费后不自动恢复——用户需手动重新绑定。

绑定/解绑逻辑不变（即时生效，无需审核）。过期后自动清空 `boundBranches = []`。

---

### 4.6 会议注册费缴纳（两阶段审核）

> **V2 核心变更：状态从4种扩展到8种，缴费流程从3步扩展到4步。**

#### 4.6.1 V2 会议注册状态机

```
          ┌──────────┐
          │  unpaid  │  ← 初始状态 / 凭证驳回后回到此状态
          └────┬─────┘
               │ submitConferenceVoucher()
               │ (上传凭证)
               ▼
          ┌─────────────────┐
          │ voucher_submitted│  ← 凭证已上传，待初审
          └────────┬────────┘
                   │
           ┌───────┼──────────┐
           │                  │
     初审通过               初审驳回
           │                  │
           ▼                  ▼
  ┌────────────────┐  ┌──────────────────┐
  │ invoice_pending │  │ voucher_rejected │
  │ (可填参会信息)   │  │ → 重新上传凭证    │
  │ 7工作日倒计时    │  └──────────────────┘
  └───────┬────────┘
          │
    ┌─────┼─────────┐
    │               │
 正常上传         逾期未上传
    │               │
    ▼               ▼
┌──────────────┐ ┌────────────────┐
│ invoice_     │ │ invoice_       │
│ submitted    │ │ overdue        │
│ (OCR比对通过)│ │ (仍可上传)      │
└──────┬───────┘ └───────┬────────┘
       │                 │
       │  ┌──────────────┘
       │  │ (上传后)
  ┌────┼──┼─────────┐
  │    │  │         │
终审通过 │       终审驳回
  │    │  │         │
  ▼    │  │         ▼
┌─────────┐│ ┌────────────────┐
│confirmed││ │ invoice_       │
│(报名完成)││ │ rejected       │
└─────────┘│ │ → 重新上传发票  │
           │ └────────────────┘
           │
    ┌──────┘
    │ (逾期后上传，同样进入终审)
```

#### 4.6.2 前置条件检查链

```
isLoggedIn?
  ├── NO → toast + 引导登录
  └── YES → 检查缴费截止
      ├── 已截止 → 按钮置灰 "缴费已截止"
      └── 未截止 → 检查会员状态
          ├── 非active → toast "请先成为学会会员"
          └── active → 检查分会绑定
              ├── 未绑定 → toast "请先绑定该分会"
              └── 已绑定 → ✅ 进入缴费流程
```

#### 4.6.3 V2 缴费流程（4步）

```
STEP 1-2: 确认金额 → 线下汇款（同V1）

STEP 3: 上传凭证（阶段一）
  → 上传转账凭证 → submitConferenceVoucher()
  → status = "voucher_submitted"

STEP 4: 上传发票（阶段二）【V2新增】
  前置条件：初审通过 → status = "invoice_pending"
  → 可填写参会信息（在发票上传前或同时）
  → 上传发票 → OCR金额比对
  → submitConferenceInvoice() → status = "invoice_submitted"
```

#### 4.6.4 V2 会议详情底部操作区（8种状态对照）

| 状态 | 缴费按钮 | 参会信息表单 | 发票上传 | 标签颜色 |
|------|---------|------------|---------|---------|
| `unpaid` | 【立即缴纳注册费报名】蓝色 | 隐藏 | 隐藏 | 灰色 |
| `voucher_submitted` | 隐藏 | 锁定 | 隐藏 | 黄色 |
| `voucher_rejected` | 【重新上传凭证】红色 | 隐藏 | 隐藏 | 红色 |
| `invoice_pending` | 隐藏 | **可编辑+发票截止日倒计时** | **开放（带截止日）** | 蓝色 |
| `invoice_overdue` | 隐藏 | **锁定** | **开放（逾期提示）** | 橙色 |
| `invoice_submitted` | 隐藏 | 锁定(只读) | 隐藏 | 黄色 |
| `invoice_rejected` | 隐藏 | 锁定(只读) | **重新开放** | 红色 |
| `confirmed` | 隐藏 | 锁定(只读) | 隐藏 | 绿色 |

**截止日倒计时实现：**

```typescript
// 内联IIFE，在 invoice_pending 状态时渲染
{(() => {
  const deadline = reg.invoiceExtendedDeadline || reg.invoiceDeadline;
  if (!deadline) return null;
  const daysLeft = Math.ceil(
    (new Date(deadline).getTime() - new Date().getTime()) / (1000 * 60 * 60 * 24)
  );
  if (daysLeft <= 0) return <span className="text-red-600">已逾期</span>;
  if (daysLeft <= 3) return <span className="text-orange-600">还有{daysLeft}天</span>;
  return <span className="text-blue-600">还有{daysLeft}天</span>;
})()}
```

---

### 4.7 参会信息与摘要提交

> **V2 变更：前置条件从 `approved_unfilled` 改为 `invoice_pending`。**

**解锁时机大幅提前：** 凭证初审通过后即可填写参会信息，不必等到发票终审通过。

**逾期后锁定：** 如果发票上传逾期（`invoice_overdue`），参会信息填写权限锁定，但已填写数据保留。

**表单字段与联动：** 与 V1 一致，无变化。

---

### 4.8 个人中心

#### 4.8.1 Tab 1: 个人信息

- 会员状态标签更新为9种新状态（含 `voucher_submitted`、`invoice_pending`、`invoice_submitted` 等）
- 状态徽章颜色按新规范

#### 4.8.2 Tab 3: 我的会议

**V2 增强：**

```
会议卡片新增字段展示：
  ├── 发票上传截止日倒计时（invoice_pending 时）
  ├── 逾期警告（invoice_overdue 时）
  ├── 凭证驳回原因 vs 发票驳回原因（独立展示）
  └── 会员到期冻结标记（frozenDueToExpiry）
```

**状态标签映射（V2 8种）：**

| 旧标签 | V2 新标签 |
|--------|----------|
| "未缴费" | "未缴费"（灰） |
| "缴费审核中" | "凭证初审中"（黄） |
| — | "凭证被驳回"（红） |
| "待填参会信息" | "待上传发票"（蓝） |
| — | "发票逾期"（橙） |
| — | "发票终审中"（黄） |
| — | "发票被驳回"（红） |
| "已报名" | "✓ 已确认"（绿） |

#### 4.8.3 Tab 4: 缴费记录

- 支付状态枚举扩展：`voucher_submitted` / `voucher_rejected` / `invoice_submitted` / `invoice_rejected`
- 新增 `rejectPhase` 字段区分凭证驳回/发票驳回
- 金额从记录中读取（不再硬编码）

---

### 4.9 通知系统

**V2 新增通知触发点（12个）：**

| # | 触发时机 | 类型 | 内容 |
|---|---------|------|------|
| 1 | 凭证初审通过（会员） | success | "凭证初审已通过，请于{deadline}前上传电子发票" |
| 2 | 凭证初审驳回（会员） | warning | "凭证被驳回：{reason}" |
| 3 | 发票终审通过（会员） | success | "会员资格已生效，有效期至{expiryDate}" |
| 4 | 发票终审驳回（会员） | warning | "发票被驳回：{reason}" |
| 5 | 凭证初审通过（会议） | success | "凭证初审已通过，请于{deadline}前上传电子发票" |
| 6 | 凭证初审驳回（会议） | warning | "凭证被驳回：{reason}" |
| 7 | 发票终审通过（会议） | success | "报名已确认" |
| 8 | 发票终审驳回（会议） | warning | "发票被驳回：{reason}" |
| 9 | 发票上传逾期 | warning | "发票上传已逾期，请尽快上传" |
| 10 | 距截止日 ≤ 3天 | warning | "发票上传截止日还有{days}天" |
| 11 | 会员到期→资格冻结 | warning | "您的会员已到期，{会议名}报名资格已冻结" |
| 12 | 会员到期→资格失效 | warning | "您的会员已到期，{会议名}报名资格已失效" |

---

### 4.10 权限模型汇总

**V2 新增两阶段权限检查链：**

```
操作                  依赖检查（逐层递进）
──────────────────────────────────────────────────────
--- 基础权限（不变）---
绑定分会              isLoggedIn → societyMembership.active
解绑分会              isLoggedIn → societyMembership.active
查看会议列表           isLoggedIn → societyMembership.active
                     → boundBranches.includes(branchId)

--- 会议缴费（两阶段新增）---
缴纳会议费（阶段一）    isLoggedIn → societyMembership.active
                     → boundBranches.includes(branchId)
                     → today <= conference.feeDeadline
                     → status ∈ {unpaid, voucher_rejected}

上传会议发票（阶段二）  isLoggedIn
                     → status ∈ {invoice_pending, invoice_overdue, invoice_rejected}
                     → today <= invoiceDeadline (或已手动延期)

填写参会信息           isLoggedIn
                     → status === "invoice_pending"
                     → 未逾期 (或已手动延期)

--- 会员缴费（两阶段新增）---
缴纳会员费（阶段一）    isLoggedIn
                     → status ∈ {not_member, voucher_rejected}

上传会员发票（阶段二）  isLoggedIn
                     → status ∈ {invoice_pending, invoice_overdue, invoice_rejected}
                     → today <= invoiceDeadline (或已手动延期)

--- 特殊操作 ---
手动延期发票截止日      isAdmin (或财务角色)
手动解锁逾期用户        isAdmin (总管理员)
```

---

## 5. 学会服务页面深度解析

### 5.4 会议服务Tab (`conference`) — V2 重构

**V2 核心变化：** 会议详情底部操作区从4种状态扩展到8种状态，每种状态有不同的渲染逻辑。

#### 8状态渲染逻辑（renderConferenceDetailBottom）

```
[unpaid]:
  收款账户信息 + 金额 + [ 立即缴纳注册费报名 ]

[voucher_submitted]:
  ⏳ 凭证初审中
  [演示] [✅ 模拟初审通过] [❌ 模拟初审驳回]

[voucher_rejected]:
  ✗ 凭证被驳回：{reason}
  [ 重新上传凭证 ]

[invoice_pending]:  ← V2 核心新增
  📋 待上传发票 | 截止日倒计时（还有X天 / 已逾期）
  + [ 填写参会与报告信息 ] 按钮（解锁！）
  + 发票上传区域
  + [演示] [✅ 模拟终审通过]

[invoice_overdue]:  ← V2 新增
  ⚠️ 发票上传已逾期 | 截止日已过X天
  + 参会信息锁定（已有数据显示为只读）
  + 发票上传区域仍然开放
  + [演示] [✅ 模拟终审通过]

[invoice_submitted]:  ← V2 新增
  ⏳ 发票终审中
  + 参会信息只读展示
  + [演示] [✅ 模拟终审通过] [❌ 模拟终审驳回]

[invoice_rejected]:  ← V2 新增
  ✗ 发票被驳回：{reason}
  + 参会信息只读展示
  + 发票重新上传区域
  + [演示] [✅ 模拟终审通过]

[confirmed]:  ← 对应旧 submitted
  ✅ 已确认 | 参会信息展示 + [ 修改参会信息 ]
```

#### 缴费截止倒计时

与发票截止日倒计时实现方式相同，在 `invoice_pending` 和 `invoice_overdue` 状态时以内联 IIFE 渲染。

---

## 9. 状态管理与数据流

### 9.1 MembershipContext V2 完整接口

```typescript
interface MembershipContextType {
  // ===== 第0层：账号（不变）=====
  currentUser: User | null;
  isLoggedIn: boolean;
  allUsers: User[];

  // ===== 第1层：会员（类型升级）=====
  societyMembership: SocietyMembership;  // 9种状态

  // ===== 第2层：分会（新增到期清空）=====
  boundBranches: string[];

  // ===== 第3层：会议（类型升级）=====
  conferenceRegs: { [confId: string]: ConferenceReg };  // 8种状态

  // ===== 通知 ====
  notifications: SystemNotification[];

  // ===== 操作函数 ====
  // 账号（不变）
  register, login, logout, updateProfile, resetPassword;

  // 会员缴费（V2 两阶段）
  submitMembershipVoucher(voucherUrl: string): void;
  submitMembershipInvoice(invoiceUrl: string): void;

  // 分会绑定（不变）
  toggleBranchBinding(branchId: string): void;

  // 会议缴费（V2 两阶段）
  submitConferenceVoucher(confId: string, voucherUrl: string, amount: number): void;
  submitConferenceInvoice(confId: string, invoiceUrl: string): void;

  // 参会信息（前置条件变更）
  submitConferenceForm, deleteAbstract, uploadAbstract;

  // V2 模拟审核（8个函数，替代旧的4个）
  simApproveSocietyVoucher(): void;
  simRejectSocietyVoucher(reason: string): void;
  simApproveSocietyInvoice(): void;
  simRejectSocietyInvoice(reason: string): void;
  simApproveConferenceVoucher(confId: string): void;
  simRejectConferenceVoucher(confId: string, reason: string): void;
  simApproveConferenceInvoice(confId: string): void;
  simRejectConferenceInvoice(confId: string, reason: string): void;

  // V2 宽限期系统
  checkInvoiceOverdue(): void;
  extendInvoiceDeadline(targetId: string, newDeadline: string, reason: string): void;

  // V2 会员到期处理
  handleMembershipExpiry(): void;
  handleMembershipRenewal(): void;

  // V2 配置读取
  getMembershipFee(memberType?: string): number;
  getConferenceFee(confId: string): number;

  // 通知（不变）
  addNotification, markNotificationRead, markAllNotificationsRead;

  // 系统（不变）
  isAdmin: boolean;
  toggleAdmin: () => void;
}
```

### 9.2 localStorage 持久化映射

| Key | 内容 | V2 变更 |
|-----|------|---------|
| `paleo_current_user_email` | string | 不变 |
| `paleo_user_db` | User[] (JSON) | 不变 |
| `paleo_society_membership_{email}` | SocietyMembership (9状态) | **类型升级** |
| `paleo_bound_branches_{email}` | string[] | 不变（到期时清空） |
| `paleo_conference_regs_{email}` | `{[confId]: ConferenceReg}` (8状态) | **类型升级** |
| `paleo_notifications_{email}` | SystemNotification[] | 不变（新增通知类型） |
| `paleo_is_admin` | boolean | 不变 |
| `paleo_membership_fee_config` | FeeConfig JSON | **V2 新增** |

### 9.3 V2 关键工作流函数

```typescript
// ── 工作日计算 ──
function calculateInvoiceDeadline(): string {
  // 从今天起加7个工作日（跳过周末）
  let days = 0, workdays = 0;
  while (workdays < 7) {
    days++;
    const d = new Date(Date.now() + days * 86400000);
    if (d.getDay() !== 0 && d.getDay() !== 6) workdays++;
  }
  return new Date(Date.now() + days * 86400000).toISOString();
}

// ── 逾期检测 ──
function checkInvoiceOverdue(): void {
  // 遍历所有 conferenceRegs + societyMembership
  // 检查 invoice_pending 且 now > invoiceDeadline
  // 自动切换: invoice_pending → invoice_overdue
  // 发出逾期通知
}

// ── 会员到期分级处理 ──
function handleMembershipExpiry(): void {
  // 遍历所有 conferenceRegs:
  //   confirmed → 保留
  //   invoice_submitted → frozenDueToExpiry = true
  //   其他 → delete
  // boundBranches = []
  // societyMembership.status = "expired"
}

// ── 续费恢复 ──
function handleMembershipRenewal(): void {
  // 遍历所有 conferenceRegs:
  //   frozenDueToExpiry → 解锁
  // (分会绑定不自动恢复)
}
```

---

## 11. 模拟数据与演示流程

### 11.1 V2 推荐演示路径（两阶段完整流程）

```
1. 注册账号 → 自动登录

2. 进入会员服务Tab → 点击"立即申请入会"
   Step 1-2: 确认金额 + 汇款确认
   Step 3: 上传凭证 → 提交 → 状态: voucher_submitted

3. [演示] 点击 "模拟初审通过"
   → 自动生成 invoiceDeadline（初审通过日+7工作日）
   → 状态: invoice_pending
   → Step 4 解锁（发票上传）

4. [演示] 点击 "模拟终审通过"
   → 状态: active（有效期1年）
   → 通知: "会员资格已生效"

5. 绑定2-3个分会

6. 切到会议Tab → 选择会议 → 查看详情
   → "立即缴纳注册费报名"
   Step 3: 上传凭证 → 提交 → 状态: voucher_submitted

7. [演示] 点击 "模拟初审通过"
   → 状态: invoice_pending → 解锁参会信息填写

8. 填写参会信息（姓名/单位/性别/身份/报告类型/摘要/住宿）
   → 提交

9. [演示] 上传发票 → OCR比对(90%模拟通过)
   → 提交 → 状态: invoice_submitted

10. [演示] 点击 "模拟终审通过"
    → 状态: confirmed → 报名完成

11. 切到个人中心 → 查看"我的会议"（新状态标签+截止日倒计时）
                → 查看"缴费记录"（区分凭证/发票提交时间）

12. [演示] 测试会员到期后的分级处理
    → 所有已确认会议保留
    → 待终审会议冻结
    → 其他会议删除
```

---

## 12. 已知问题与待改进

### 12.1 V1 已解决的问题

| 原问题 | V2 解决方案 | 状态 |
|--------|-----------|------|
| 分会ID 3套不统一 | 提取到 `shared/constants.ts` 的 `BRANCH_MAP` | ✅ 已解决 |
| 会议ID 2套不统一 | 提取到 `shared/constants.ts` 的 `CONFERENCE_BRANCH_MAP` | ✅ 已解决 |
| 凭证+发票同步骤上传，无独立审核 | 拆分为两阶段独立审核 | ✅ 已解决 |
| 发票选传，无追缴机制 | 必传 + 7工作日宽限期 | ✅ 已解决 |
| 会员到期后会议资格无处理规则 | 分级处理（保留/冻结/删除） | ✅ 已解决 |
| 会员费金额硬编码 | 改为 `getMembershipFee()` 配置读取 | ✅ 已解决 |
| expired状态颜色不一致（灰 vs 黄） | 统一为灰色 | ✅ 已解决 |
| "加入会员"按钮未直接进入缴费 | 保留当前行为（会员Tab引导），通过两阶段优化体验 | ✅ 已明确 |

### 12.2 V2 代码结构待优化

| 问题 | 严重度 | 建议 |
|------|--------|------|
| Services.tsx 超过2500行 | 🔴 高 | 拆分缴费流程为独立组件 |
| 两阶段缴费逻辑与旧函数并存 | 🟡 中 | V3 移除 deprecated wrappers |
| 多处内联IIFE倒计时渲染 | 🟡 中 | 提取为 `InvoiceDeadlineCountdown` 组件 |

### 12.3 原型阶段缺失的功能（正常，V2未变更）

| 缺失功能 | 说明 |
|---------|------|
| 后台管理系统 | CMS/审核/统计 — 非原型范围 |
| OCR 真实识别 | 当前为随机模拟（90%通过率） |
| 邮件发送 | 仅站内toast + 通知铃铛 |
| 文件真实上传/下载 | 模拟文件名 |
| 在线支付 | 当前为全线下转账模式 |

### 12.4 V2 新增待确认事项

1. **手动延期是否需要操作日志在前端展示？** — 当前仅在后台记录
2. **发票逾期后是否收取滞纳金？** — 需求未明确
3. **宽限期的法定节假日是否需要排除？** — 当前仅排除周末
4. **OCR识别失败时的降级方案？** — 当前模拟阶段不存在此问题

---

## 13. V1→V2 变更明细

### 13.1 文件变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `shared/constants.ts` | **新增** | 共享常量：配置、枚举、标签、映射 |
| `MembershipContext.tsx` | **重大重构** | 两阶段状态机、8个审核函数、宽限期、会员到期处理 |
| `Services.tsx` | **重大修改** | 会员4步缴费、会议8状态UI、截止日倒计时 |
| `PersonalCenter.tsx` | **修改** | 新状态标签、截止日倒计时、驳回理由区分 |

### 13.2 接口变更

| 旧接口 | 新接口 | 说明 |
|--------|--------|------|
| `applySocietyMembership(voucher, invoice, amount)` | `submitMembershipVoucher(voucher)` + `submitMembershipInvoice(invoice)` | 拆分为两阶段 |
| `payConference(confId, voucher, invoice, amount)` | `submitConferenceVoucher(confId, voucher, amount)` + `submitConferenceInvoice(confId, invoice)` | 拆分为两阶段 |
| `simApproveSocietyMembership()` | `simApproveSocietyVoucher()` + `simApproveSocietyInvoice()` | 初审/终审分离 |
| `simRejectSocietyMembership(reason)` | `simRejectSocietyVoucher(reason)` + `simRejectSocietyInvoice(reason)` | 初审/终审分离 |
| `simApproveConference(confId)` | `simApproveConferenceVoucher(confId)` + `simApproveConferenceInvoice(confId)` | 初审/终审分离 |
| `simRejectConference(confId, reason)` | `simRejectConferenceVoucher(confId, reason)` + `simRejectConferenceInvoice(confId, reason)` | 初审/终审分离 |
| (无) | `checkInvoiceOverdue()` | 新增 |
| (无) | `handleMembershipExpiry()` | 新增 |
| (无) | `handleMembershipRenewal()` | 新增 |
| (无) | `extendInvoiceDeadline(id, deadline, reason)` | 新增 |
| (无) | `getMembershipFee(type)` | 新增 |
| (无) | `getConferenceFee(confId)` | 新增 |

### 13.3 状态映射

| V1 状态 | V2 状态 | 语义 |
|---------|---------|------|
| `unpaid` | `unpaid` | 不变 |
| `pending` (会议) | `voucher_submitted` | 细化 |
| — | `voucher_rejected` | 新增 |
| — | `invoice_pending` | 新增 |
| — | `invoice_overdue` | 新增 |
| — | `invoice_submitted` | 新增 |
| — | `invoice_rejected` | 新增 |
| `approved_unfilled` | 删除 | 合并到 `invoice_pending` |
| `submitted` | `confirmed` | 重命名 |
| `not_member` | `not_member` | 不变 |
| `pending` (会员) | `voucher_submitted` | 细化 |
| `rejected` (会员) | `voucher_rejected` / `invoice_rejected` | 区分阶段 |
| `active` | `active` | 不变 |
| `expired` | `expired` | 不变 |
